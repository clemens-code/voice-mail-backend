import {DataTypes, Model} from "sequelize";
import {sequelize} from "../audioRecord.repo";

/*export class AudioRecord extends Model {
}

AudioRecord.init({
        id: {
            type: DataTypes.UUID,
            defaultValue: DataTypes.UUIDV4,
            allowNull: false,
        },
        avatarSource: {
            type: DataTypes.STRING,
            allowNull: true
        },
        avatarAlt: {
            type: DataTypes.STRING,
            allowNull: true
        },
        bgColor: {
            type: DataTypes.STRING,
            allowNull: false
        },
        title: {
            type: DataTypes.STRING,
            allowNull: false
        },
        description: {
            type: DataTypes.STRING,
            allowNull: false
        },
        userName: {
            type: DataTypes.STRING,
            allowNull: false
        },
        audioUrl: {
            type: DataTypes.STRING,
            allowNull: true
        },
    },
    {
        sequelize,
        modelName: 'AudioRecord'
    });*/
